VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
VITE_MAPBOX_TOKEN=pk.your-mapbox-public-token
VITE_DEV_LOGIN_ENABLED=true

# 本地开发可留空，继续使用 Vite /api 和 /ws 代理。
# Vercel 生产默认值配置在 frontend/deploy.config.json，不需要在 Vercel UI 配置这些公开变量。
VITE_API_BASE_URL=
VITE_WS_BASE_URL=
