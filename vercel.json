{
  "framework": "vite",
  "installCommand": "corepack enable && corepack prepare pnpm@9.12.3 --activate && cd frontend && pnpm install --frozen-lockfile",
  "buildCommand": "cd frontend && node scripts/ensure-production-env.mjs && pnpm build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
