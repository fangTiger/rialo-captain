{
  "framework": "vite",
  "installCommand": "cd frontend && corepack pnpm@9.12.3 install --frozen-lockfile",
  "buildCommand": "cd frontend && node scripts/ensure-production-env.mjs && corepack pnpm@9.12.3 build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
